# Microcontroller-based-Flight-Sim
 Real-time 2D game on ARM Cortex-M0+, implementing custom C drivers for SPI LCD, ADC using slide potentiometer (thrust), and DAC speaker (audio). Implemented interrupt-driven I/O for responsive GPIO button inputs to control sprite rendering at high refresh rates.
